const Promise = require("bluebird");

/*
 * children of the same branch on the word tree
 * should never be mixed together
 */
module.exports = generateCombinations = function (wordTree) {
    const Promise = require("bluebird");
};